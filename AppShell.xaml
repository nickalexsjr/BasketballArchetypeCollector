<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="BasketballArchetypeCollector.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:BasketballArchetypeCollector.Views"
    Shell.FlyoutBehavior="Disabled"
    Shell.NavBarIsVisible="False"
    Shell.TabBarBackgroundColor="#0f172a"
    Shell.TabBarTitleColor="#f97316"
    Shell.TabBarUnselectedColor="#64748b">

    <!-- Login/Signup screen - no tabs visible -->
    <ShellItem Route="login" FlyoutItemIsVisible="False">
        <ShellContent ContentTemplate="{DataTemplate views:SignInPage}" />
    </ShellItem>

    <!-- Main app with tabs -->
    <TabBar Route="main">
        <Tab Title="Packs" Route="packs">
            <Tab.Icon>
                <FontImageSource Glyph="📦" FontFamily="InterRegular" />
            </Tab.Icon>
            <ShellContent ContentTemplate="{DataTemplate views:PackStorePage}" />
        </Tab>

        <Tab Title="Collection" Route="collection">
            <Tab.Icon>
                <FontImageSource Glyph="🃏" FontFamily="InterRegular" />
            </Tab.Icon>
            <ShellContent ContentTemplate="{DataTemplate views:CollectionPage}" />
        </Tab>

        <Tab Title="Database" Route="database">
            <Tab.Icon>
                <FontImageSource Glyph="📊" FontFamily="InterRegular" />
            </Tab.Icon>
            <ShellContent ContentTemplate="{DataTemplate views:DatabasePage}" />
        </Tab>

        <Tab Title="Stats" Route="stats">
            <Tab.Icon>
                <FontImageSource Glyph="📈" FontFamily="InterRegular" />
            </Tab.Icon>
            <ShellContent ContentTemplate="{DataTemplate views:StatsPage}" />
        </Tab>
    </TabBar>

</Shell>
